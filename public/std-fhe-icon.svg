<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
  <!-- Define circle mask to make outside transparent -->
  <defs>
    <mask id="circleMask">
      <rect width="100" height="100" fill="none"/>
      <circle cx="50" cy="50" r="48" fill="white"/>
    </mask>
  </defs>

  <!-- Background: transparent square with white circle -->
  <rect width="100" height="100" fill="none" mask="url(#circleMask)"/>

  <!-- White circle with very thin dark gray stroke -->
  <circle cx="50" cy="50" r="48" fill="none" stroke="#333" stroke-width="0.5"/>

  <!-- Monospaced text in dark gray -->
  <text x="50%" y="55%" text-anchor="middle" font-family="monospace"
        font-size="18" fill="#333">std::fhe</text>
</svg>
